name: Tests

on:
  push:
    paths-ignore:
      - "**.md"

jobs:
  build:
    runs-on: "ubuntu-latest"

    steps:
      - name: "Checkout code"
        uses: "actions/checkout@v7"

      - name: "Prepare Java"
        uses: "actions/setup-java@v5"
        with:
          distribution: "temurin"
          java-version: "26"

      - name: "Prepare Clojure"
        uses: "DeLaGuardo/setup-clojure@master"
        with:
          bb: "latest"

      - name: "Apply Cache"
        uses: "actions/cache@v5"
        with:
          path: |
            ~/.m2/repository
          key: "claxon-${{ hashFiles('deps.edn') }}"
          restore-keys: "$claxon-"

      - name: "Run JVM tests"
        run: "bb test:jvm"

      - name: "Run bb tests"
        run: "bb test:bb"
